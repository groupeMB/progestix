{% extends "PharmacieAccueilBundle:Template:template.html.twig" %}
{% block contenu %}
    
    {% for type, messages in app.session.flashBag.all %}
        {% for message in messages %}
            <div class="{{ type }}">
                {{ message|trans({}, 'FOSUserBundle') }}
            </div>
        {% endfor %}
    {% endfor %}


    {% if error %}
    	<div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}
	<div class="container">
	<div class="row" style="margin-top:200px;">
		<div class="col-md-6">
			<div class="panel panel-success">
  				<div class="panel-heading"><center><span class="glyphicon glyphicon-info-sign"></span> Le leadership du pharmacien</center></div>
  				<div class="panel-body">
		   			<ul class="list-group">
					  <li class="list-group-item">1. Penser et réagir en focalisant toutes vos actions sur la problématique principale de la pharmacie</li>
					  <li class="list-group-item">2. Etablir des objectifs et des tactiques pour chaque catégorie basés sur la rotation et les chiffres d'affaire</li>
					  <li class="list-group-item">3. Comprendre la variabilité du travail à travers la planification et la résolution des problèmesComprendre comment les individus apprennent, se développent et progressent eux-mêmes</li>
					  <li class="list-group-item">4. Comprendre les autres et leurs comportements</li>
					</ul>
			  	</div>
			</div>
		</div>
	    <div class="col-md-5">
	    	<div class="panel panel-success">
  				<div class="panel-heading"><center><span class="glyphicon glyphicon-log-in"></span> Accès à votre tableau de bord sécurisé</center></div>
  				<div class="panel-body">
		   			<form role="form" action="{{ path("fos_user_security_check") }}" method="post">
						<fieldset>
							
							<div class="form-group">
			                    <input type="text" name="_username" id="email" class="form-control input" placeholder="Identifiant">
							</div>
							<div class="form-group">
			                    <input type="password" name="_password" id="password" class="form-control input" placeholder="Mot de passe">
							</div>
							<a href="" class="btn btn-link pull-right">Mot de passe oublié?</a>
							</br>
							<hr class="colorgraph">
							<div class="row">
								<div class="col-xs-6 col-sm-6 col-md-6">
			                        <input type="submit" class="btn btn-success btn-block" value="Connexion">
								</div>
								<div class="col-xs-6 col-sm-6 col-md-6">
									<a href="" class="btn btn-primary btn-block">Reset</a>
								</div>
							</div>
						</fieldset>
					</form>

			  	</div>
			</div>
		</div>

		
	</div>
{% endblock %}